import { FC } from 'react';

export const Header: FC = () => {
  return (
    <div className="bg-primary text-white">
      <div className="text-3xl font-calligraffitti">{'D&P'}</div>
    </div>
  );
};
